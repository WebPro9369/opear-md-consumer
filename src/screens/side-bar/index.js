import React from 'react';
import {
	ContainerView
} from './styles';

class SideBar extends React.PureComponent {
	render () {
		return (
			<ContainerView>
			</ContainerView>
		)
	}
};

export default SideBar;
